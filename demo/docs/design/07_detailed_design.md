# 07 详细设计（Detailed Design）

适用对象：国央企 / 政企用户（强调流程合规、权限边界、审计留痕、口径一致）

## 1. 模块说明
对每个模块/页面给出可直接指导原型开发的信息。

### 1.1 模块清单（占位）
- 模块 A：（例如台账管理）
- 模块 B：（例如流程审批）
- 模块 C：（例如组织与权限 / 审计留痕）

## 2. 页面与交互设计（建议逐页编写）
### 2.1 页面：XXX 列表页（示例占位）
- 访问路径：
- 权限要求（角色/部门/数据范围）：
- 查询条件：
- 列表字段：
- 操作按钮与权限：
- 状态定义（loading/empty/error/success）：
- 异常处理与提示语：
- 审计留痕点位（查询、导出、关键操作）：

### 2.2 页面：XXX 详情/表单页（示例占位）
- 访问路径：
- 字段与校验规则：
- 枚举与字典来源：
- 提交/保存规则：
- 审批/流转规则（如适用）：
- 审计留痕点位（新增、修改、提交、审批）：

## 3. 接口与契约（OpenAPI 为准）
- 契约文件：`docs/design/openapi.yaml`
- 约束：service/types/mock 必须与 OpenAPI 一致

### 3.1 接口清单（占位）
| 接口 | 方法 | 说明 | 权限 | 备注 |
| --- | --- | --- | --- | --- |
| /xxx | GET | 查询 | 角色/数据范围 | 分页 |

### 3.2 错误码与提示语规范（占位）
- 错误码格式：
- 前端提示语原则：可理解、可定位、可追溯（必要时显示错误码）

## 4. 关键流程时序（占位）
用文字或时序图描述：发起 → 审批 → 归档 / 导出 等关键链路。

## 5. 日志与审计（原型约束）
- 前端埋点/审计留痕字段（占位）：操作人、时间、对象、结果、请求参数摘要
- 导出审计：导出条件、导出文件标识（占位）
